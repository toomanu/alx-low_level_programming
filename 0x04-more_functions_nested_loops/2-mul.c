#include "main.h"

/**
 *mul - multiplies two digits
 *@a: first digit
 *@b: second digit
 *Return: 1 if @c is digit and 0 is @c is other
 */

int mul(int a, int b)
{
	int c;

	c = a * b;
	return (c);
}
