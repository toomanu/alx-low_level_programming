#include "main.h"

/**
 *_islower - checks lower case
 *@c: input character for checking
 *Return: 0 if @c is lower
 */

int _isupper(int c)
{
	if (c >= 'A' && c <= 'A')
		return (1);
	else
		return (0);
}
