#include "main.h"

/**
 *_isupper - checks upper case
 *@c: input character for checking
 *Return: 0 if @c is upper
 */

int _isupper(int c)
{
	if (c >= 'A' && c <= 'A')
		return (1);
	else
		return (0);
}
