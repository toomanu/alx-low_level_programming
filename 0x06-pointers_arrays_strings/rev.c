#include "main.h"

/**
 *_strcat - concatenates two strings
 *@dest: first string
 *@src: inpsecond string
 *Return: dest
 */

void rev_string(char *s)
{   
    int i, len, temp;  

	while (*s)
	len++
    
      
    for (i = 0; i < len/2; i++)  
    {  
     
        temp = s[i];  
        s[i] = str1[len - i - 1];  
        s[len - i - 1] = temp;  
    }  
}
