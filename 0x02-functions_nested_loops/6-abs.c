#include "main.h"

/**
 * _abs - returns absolute value of integer
 * @n: input character for checking
 * Return: absolute int
 */

int _abs(int n)
{

	if (n >= 0)
	{
	return (n);
	}
	else
	n = (n * -1);
	return (n);
}
