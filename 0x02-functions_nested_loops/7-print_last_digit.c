#include "main.h"

/**
 * print_last_digit - print last digit
 * @n: input character for checking
 * Return: absolute int
 */

int print_last_digit(int n)
{
	n = (n % 10);
	_putchar (n);
	return (0);
}
