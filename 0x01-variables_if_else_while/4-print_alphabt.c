#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - print alphabet
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int a;
	for (a = 'a'; a <= 'z'; a++)
		{
			putchar(a,b,c);
		}
	putchar('\n');
	return (0);
}
